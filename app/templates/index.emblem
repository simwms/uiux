#application-index.full-screen-modal
  .modal-container
    .container
      .row
        .col-lg-6.col-md-8.col-sm-10.col-xs-12.col-lg-offset-3.col-md-offset-2.col-md-offset-1
          .modal-box
            if currentUser.isLoggedIn
              == partial "sessions/chose"
            else
              == partial "sessions/login"
